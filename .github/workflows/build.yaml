name: web
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install
        run: yarn
      - name: build
        run: yarn web build
      - name: Create .env with GH secrets
        uses: SpicyPizza/create-envfile@v1
        with:
          envkey_S3_ID: ${{ secrets.S3_ID }}
          envkey_S3_SECRET: ${{ secrets.S3_SECRET }}

