{
  "name": "gear",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gear",
    "create-local": "graph create --node http://localhost:8020/ gear",
    "remove-local": "graph remove --node http://localhost:8020/ gear",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gear"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  },
  "version": "1.0.0",
  "main": "index.js",
  "author": "tarrenceva",
  "description": ""
}
