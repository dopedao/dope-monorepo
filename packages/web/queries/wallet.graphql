query Wallet($id: ID!) {
  wallet(id: $id) {
    id
    address
    paper
    bags {
      id
      clothes
      foot
      hand
      drugs
      neck
      ring
      vehicle
      waist
      weapon
      claimed
    }
  }
}
