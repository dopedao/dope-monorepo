query Wallet($id: ID!) {
  wallet(id: $id) {
    id
    address
    paper
    bags(first: 5) {
      id
      clothes
      foot
      hand
      drugs
      neck
      ring
      vehicle
      waist
      weapon
      claimed
    }
  }
}
