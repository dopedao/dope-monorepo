query Bags($first: Int, $last_id: ID) {
  bags(first: $first, where: { id_gt: $last_id }) {
    id
    clothes
    foot
    hand
    drugs
    neck
    ring
    vehicle
    waist
    weapon
    claimed
    rank @client
  }
}