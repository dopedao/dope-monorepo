// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/dopedao/dope-monorepo/packages/api/ent/syncstate"
)

// SyncState is the model entity for the SyncState schema.
type SyncState struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// StartAt holds the value of the "start_at" field.
	StartAt uint64 `json:"start_at,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SyncState) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case syncstate.FieldStartAt:
			values[i] = new(sql.NullInt64)
		case syncstate.FieldID:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type SyncState", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SyncState fields.
func (ss *SyncState) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case syncstate.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				ss.ID = value.String
			}
		case syncstate.FieldStartAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field start_at", values[i])
			} else if value.Valid {
				ss.StartAt = uint64(value.Int64)
			}
		}
	}
	return nil
}

// Update returns a builder for updating this SyncState.
// Note that you need to call SyncState.Unwrap() before calling this method if this SyncState
// was returned from a transaction, and the transaction was committed or rolled back.
func (ss *SyncState) Update() *SyncStateUpdateOne {
	return (&SyncStateClient{config: ss.config}).UpdateOne(ss)
}

// Unwrap unwraps the SyncState entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ss *SyncState) Unwrap() *SyncState {
	tx, ok := ss.config.driver.(*txDriver)
	if !ok {
		panic("ent: SyncState is not a transactional entity")
	}
	ss.config.driver = tx.drv
	return ss
}

// String implements the fmt.Stringer.
func (ss *SyncState) String() string {
	var builder strings.Builder
	builder.WriteString("SyncState(")
	builder.WriteString(fmt.Sprintf("id=%v", ss.ID))
	builder.WriteString(", start_at=")
	builder.WriteString(fmt.Sprintf("%v", ss.StartAt))
	builder.WriteByte(')')
	return builder.String()
}

// SyncStates is a parsable slice of SyncState.
type SyncStates []*SyncState

func (ss SyncStates) config(cfg config) {
	for _i := range ss {
		ss[_i].config = cfg
	}
}
